// Where the canonical dataset lives on GitHub. The add-match flow copies the
// updated JSON and opens this editor so the commit happens on github.com —
// the app itself never writes anywhere (v1 has no backend; see ENGINE.md).
export const DATASET_EDIT_URL =
  "https://github.com/alanwongsai/DeuceLine/edit/main/public/data/deuceline-data.json";
