node_modules/
dist/
.DS_Store
*.tsbuildinfo
node_modules/.tmp/
.playwright-cli/

# Cloudflare Pages local dev — real secrets and wrangler state, never commit
.dev.vars
.wrangler/

# Local Claude Code config (machine-specific settings, preview launch config)
.claude/

# Shared-core bundles for the mini program (npm run build:core)
dist-core/

# WeChat DevTools per-developer settings
project.private.config.json
